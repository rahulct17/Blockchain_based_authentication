<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>Daily Journa Loginl</title>
  <meta charset="utf-8">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <nav class="navbar navbar-default">
    <div class="container">
      <div class="navbar-header">
        <p class="navbar-brand">DAILY JOURNAL</p>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <!-- 6 -->
        <li id="/"><a href="/">HOME</a></li>
        <li id="about"><a href="/about">ABOUT US</a></li>
        <li id="contact"><a href="/contact">CONTACT US</a></li>
      </ul>
    </div>
  </nav>


  <link rel="canonical" href="https://getbootstrap.com/docs/4.5/examples/sign-in/">

  <!-- Bootstrap core CSS -->
  <link href="/docs/4.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

  <!-- Favicons -->

  <style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      -ms-user-select: none;
      user-select: none;
    }

    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }
  </style>
  <!-- Custom styles for this template -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="text-center">

  <!-- <form class="form-signin">
    <img class="mb-4" src="https://image.flaticon.com/icons/svg/889/889057.svg" alt="" width="72" height="72">
    <!-- <button class="btn btn-lg btn-primary btn-block btn-warning" action="/home" onclick="validateUser()">Login</button> -->
    <!-- <button class="btn btn-lg btn-primary btn-block btn-success" onclick="sendData()">Register</button>
  </form> -->
  <form class="form-signin log" method="post" >
     <!-- <img class="mb-4" src="https://image.flaticon.com/icons/svg/889/889057.svg" alt="" width="72" height="72"> -->
    <button class="btn btn-lg btn-primary btn-block btn-warning"  onclick="validateUser()">Login</button>
    <!-- <button class="btn btn-lg btn-primary btn-block btn-success" onclick="sendData()">Register</button> -->
  </form>

  <p class="mt-5 mb-3 text-muted">&copy;Rahul Thakur @2020</p>
  <%-include("partials/footer")%>

  <!-- <script>
    async function sendData() {

      // Get Ethereum Address of user's account

      // Modern dapp browsers...
      if (window.ethereum) {
        web3Provider = window.ethereum;
        try {
          // Request account access
          await window.ethereum.enable();
          console.log(web3Provider.selectedAddress);
        } catch (error) {
          // User denied account access...
          console.error("User denied account access")
        }
      }
      // Legacy dapp browsers...
      else if (window.web3) {
        web3Provider = window.web3.currentProvider;
        console.log(web3Provider)
      }
      // If no injected web3 instance is detected, fall back to Ganache
      else {
        console.log("No Web Client Detected, hence cannot log in")
      }
      web3 = new Web3(web3Provider);

      // Send XMLHttpRequest with parameters

      let xhr = new XMLHttpRequest()
      let json = JSON.stringify({
        address: web3Provider.selectedAddress,
      })

      xhr.open('POST', '/login', true)
      xhr.setRequestHeader('Content-type', 'application/x-www-form-urlencoded')
      xhr.send("address=" + web3Provider.selectedAddress)



      xhr.onreadystatechange = function() {
        if (xhr.readyState === 4) {
          document.body.innerHTML = xhr.response
        }
      }
    }


    // }
  </script> -->
